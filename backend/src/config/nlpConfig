module.exports = {
  engine: 'rasa',  // Definisci il motore NLP, in questo caso Rasa
  rasa: {
    endpoint: 'http://localhost:5005/webhooks/rest/webhook',  // URL dell'endpoint di Rasa (modifica se è remoto)
    sender: 'user',  // Nome del mittente (puoi usare una sessione univoca per l'utente)
    // Puoi aggiungere ulteriori configurazioni se necessarie
  }
};
